import 'react';
import { AnimatedTag } from './AnimatedTag.web.js';
import { jsx } from 'react/jsx-runtime';

var getTagsGroup = function getTagsGroup(_ref) {
  var tags = _ref.tags,
    activeTagIndex = _ref.activeTagIndex,
    isDisabled = _ref.isDisabled,
    onDismiss = _ref.onDismiss,
    _ref$size = _ref.size,
    size = _ref$size === void 0 ? 'medium' : _ref$size;
  return tags.map(function (tagName, tagIndex) {
    return /*#__PURE__*/jsx(AnimatedTag, {
      activeTagIndex: activeTagIndex,
      onDismiss: onDismiss,
      currentTagIndex: tagIndex,
      tagsLength: tags.length,
      isDisabled: isDisabled,
      size: size,
      children: tagName
    }, "".concat(tagName, "-").concat(tagIndex));
  });
};

export { getTagsGroup };
//# sourceMappingURL=getTagsGroup.js.map
