import React__default from 'react';
import '../../utils/logger/index.js';
import { throwBladeError } from '../../utils/logger/logger.js';

var TooltipContext = /*#__PURE__*/React__default.createContext(null);
var useTooltipContext = function useTooltipContext() {
  var context = React__default.useContext(TooltipContext);
  if (false) {
    if (!context) {
      throwBladeError({
        message: "TooltipInteractiveWrapper must be used within Tooltip",
        moduleName: 'Tooltip'
      });
    }
  }
  return context;
};

export { TooltipContext, useTooltipContext };
//# sourceMappingURL=TooltipContext.js.map
