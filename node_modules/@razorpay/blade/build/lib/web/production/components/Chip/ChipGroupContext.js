import React__default from 'react';
import '../../utils/logger/index.js';
import { throwBladeError } from '../../utils/logger/logger.js';

var ChipGroupContext = /*#__PURE__*/React__default.createContext({});
var ChipGroupProvider = ChipGroupContext.Provider;
var useChipGroupContext = function useChipGroupContext() {
  var context = React__default.useContext(ChipGroupContext);
  if (false) {
    if (typeof context === 'undefined') {
      throwBladeError({
        message: 'useChipGroupContext must be used within ChipGroup',
        moduleName: 'ChipGroup'
      });
    }
  }
  return context;
};

export { ChipGroupProvider, useChipGroupContext };
//# sourceMappingURL=ChipGroupContext.js.map
