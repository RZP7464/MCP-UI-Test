import React__default from 'react';
import '../../utils/logger/index.js';
import { throwBladeError } from '../../utils/logger/logger.js';

var PopoverContext = /*#__PURE__*/React__default.createContext(null);
var usePopoverContext = function usePopoverContext() {
  var context = React__default.useContext(PopoverContext);
  if (true) {
    if (!context) {
      throwBladeError({
        message: "usePopoverContext must be used within Popover",
        moduleName: 'Popover'
      });
    }
  }
  return context;
};

export { PopoverContext, usePopoverContext };
//# sourceMappingURL=PopoverContext.js.map
